import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from "react-native";


const AppointmentManagement = () => {
    return (
        <SafeAreaView>
            <Text>Quản lí lịch hẹn</Text>
        </SafeAreaView>
    )
}
export default AppointmentManagement;